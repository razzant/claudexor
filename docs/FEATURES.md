# Feature Status Ledger

This file tracks every user-facing feature that is NOT currently in a `solid`
state. It exists so an agent touching a feature inherits the known state
instead of rediscovering it. Rule (see `CONTRIBUTING.md`): when a change
alters a feature listed here, update or delete its row in the same commit. A
feature that becomes solid loses its row — this ledger shrinks toward empty;
growth is a regression signal.

Statuses: `broken` > `dead` (wired to nothing) > `half-baked` > `suspicious` >
`works-with-caveats`. `Planned` names the stabilization-program phase that
owns the fix (`backlog` = not yet scheduled). Evidence is file:line at the
time of the audit; lines drift with edits — verify before relying on them.

Rows: **15** (works-with-caveats: 15)

| Area | Feature | Status | What is wrong / caveat | Evidence | Planned |
|---|---|---|---|---|---|
| engine/models | Caller-owned Codex model operations | works-with-caveats | Codex is the implemented raw-model source; other connected harnesses remain Agents. The backend rejects explicit maxOutputTokens and temperature. Incremental cash remains unknown because this transport supplies no cash receipt. Native continuation requires an observed account fingerprint and model; missing identity is not invented. A crash between response publication and terminal journal acceptance leaves an unknown outcome. Controlled tests cover transport, custody and managed-auth behavior; platform CI and the embedding application's complete live acceptance remain separate evidence. | packages/harness-codex/src/model.ts, model-auth.ts, responses.ts; packages/daemon/src/model-operations.ts; docs/ARCHITECTURE.md caller-owned model operations | affected-platform and embedding acceptance |
| engine/web-evidence | Codex web-retrieval verification (QA-042) | works-with-caveats | Codex `exec --json` `web_search`/`open_page` items carry NO typed fetch outcome — a `completed` item can hide a 502 Bad Gateway. Claudexor therefore stamps codex web results `web_retrieval: dispatched` (the call completed) and records `web.verification: dispatched`, NOT `verified`: a surface must render "Web reached", never "Web verified", for a codex-only web route. Web is optional, so this strength is observational rather than a terminal gate. Claude's WebSearch/WebFetch expose typed content and stamp `verified`. Closing the codex gap needs an upstream typed result (the July-2026 0.145 "structured standalone web search results" changelog is a lead, unverified on `exec --json`). Do NOT prose-parse the answer to recover the outcome (INV-049) | packages/harness-codex/src/parse.ts (web_search `web_retrieval: dispatched`); packages/orchestrator/src/attemptTelemetry.ts (`bumpWebVerification`, `web.verification`); packages/harness-codex/fixtures/basic-run.jsonl (completed web_search carries only status) | Ф3 (macOS label) / upstream |
| engine/work-report | D-16 WorkReport / work_state axis (v3.1 Ф1) | works-with-caveats | D-16a–d ship the schema contract, the transport envelope, the unified finalizer, the work_state veto, the outcome-aware exit, the adapter context signals, and the one-shot continuation. The transport is now ACTIVE on all three tiers: `constrained` (codex `--output-schema`, claude `--json-schema` with a caller schema), claude `side_tool` (a `{work_report}`-only StructuredOutput tool while the markdown final stays the deliverable), and cursor `validated` (the complete markdown final followed by an instructed fenced `{work_report}` metadata block — a missing/malformed footer is a typed `work_report_contract` failure; historical fence-only `{work_report, output}` replies remain readable). Claude maps the fixture-proven context signals (`terminal_reason` prompt_too_long/rapid_refill_breaker → capacity_exhausted, `compact_boundary` → compaction, the typed `rate_limit_event`). Remaining caveats: codex context-exhaustion stays honestly GENERIC — the 0.153.3 oversized-input capture surfaces a stderr JSON-RPC error (`input_error_code: input_too_large`) before model execution, with no typed context stream frame. This character-limit capture is not token-window evidence, and the adapter has no token-window context mapping; claude's `--json-schema` × interactive stream-json lane still discloses the transport `unsupported`; and the continuation is eligible only for claude `repeated_refill` (prompt_too_long is treated as an irreducible packet). Codex-specific display note (openai/codex #19816): `--output-schema` constrains INTERMEDIATE agent messages too, not just the final one, so a mid-run narration arrives as the raw `{work_report, output}` envelope — the codex adapter now typed-detects that shape (exact `work_report`+`output` keys, `work_report` matching the WorkReport schema) and unwraps it to its `output` for the visible stream (suppressing a non-string `output`), so raw envelope JSON never reaches the progress bubble; the FINAL message likewise unwraps its DISPLAY text to `output` (so the answer bubble and the reducer's twin-removal see the same unwrapped text the intermediate copy carried — the QA-009 twin fix), while the RAW `{work_report, output}` envelope rides `payload.work_report_envelope`; the orchestrator reads machine truth via `AnswerAssembly.machineText()`, and a non-string/non-envelope final keeps its raw text with `machineText()` falling back to it. Claude (`side_tool`) and cursor (instructed fence on the final answer only) never carry the envelope in the intermediate text stream, so this is codex-only | packages/harness-claude/src/parse.ts (context + rate_limit + side_tool); packages/harness-codex/src/parse.ts (`detectEnvelopeOutput` intermediate unwrap/suppress; `CodexParseState.envelopeActive`); packages/harness-codex/fixtures/structured-envelope-intermediate.jsonl; packages/orchestrator/src/attemptFinalize.ts (`resolveWorkReportEnvelope` activates all tiers); packages/orchestrator/src/continuation.ts; packages/harness-codex/fixtures/context/exec-oversized-input.stderr.txt (documented upstream gap) | Ф1 (dogfood) |
| macos/updater | Engine-runtime auto-install (SHIPPED in 3.1.0 — D-2) | works-with-caveats | One-click in-app update: signed runtime manifest (offline Ed25519 authority, pinned key, fail-closed verify), download → sha256-verify → unpack → busy-gate (refuses while runs/jobs are active or setup process termination remains unconfirmed) → identity-stop the daemon → atomic current.json pointer swap under flock → relaunch → handshake-verify → rollback to last-known-good on ANY post-swap failure; bundled runtime stays the final fallback. The same single closure now materializes internal package links into a regular-file/directory-only archive for exact-pinned host embedding, without adding another payload or trust root. Exact-candidate-artifact promotion in the release workflow (candidate_run_id + gh attestation verify + byte-identity). `claudexor release check` verifies the same signed manifest fail-closed. A setup failure recorded before process-group evidence can leave replacement and further login for that harness deferred indefinitely; Reconcile, Cancel and restart cannot clear that historical record, and automatic recovery is deferred | apps/macos/.../RuntimeInstallCoordinator.swift, RuntimeUpdater.swift, RuntimeUpdate.swift; scripts/build-runtime-closure.mjs; scripts/lib/runtime-manifest-contract.mjs; packages/util/src/runtime-manifest.ts | — |
| engine/convergence | Typed run-failure refusal in the `until-clean` lane | works-with-caveats | A typed pre-spawn refusal (`subscription_window_exhausted` with its structural `resetsAt`) is written typed in EVERY strategy lane: quota admission runs during ROUTING (`preflightProfile` inside `resolveCandidateAdapters`), and the race, convergence, Plan, and read-only routing catches all call `writeRoutingFailureTerminal`, which preserves the declared `code` and `resetsAt` in `failure.yaml` — a scheduler driving `--until-clean` reads the refusal typed, never from prose. A MID-ATTEMPT typed refusal (spent window, and since A5 the `credential_pool_exhausted` terminal a reactive rotation-exhaustion declares through normal attempt finalization) now survives in EVERY lane: the race per-slot catch and unanimity terminal, `runConvergence`'s per-attempt catch plus its last-result terminal, and the read-only chain terminal (which speaks with the LAST attempt's declared refusal). Explicit review-off capped repair retains deterministic checks, policy findings and work completion without starting model reviewers. Remaining caveats: the convergence terminal lifts the declared code only when the terminal is a plain failure — budget, cancel, and needs-decision authority still mask it (e.g. `allCooledDown` maps to `budget_exhausted` before the declared code is consulted) — and the read-only lift carries the last attempt's refusal only, mirroring its prose terminal | packages/orchestrator/src/routing-failure.ts (`routingFailureClassification` + `writeRoutingFailureTerminal` preserve code+resetsAt); packages/orchestrator/src/orchestrator.ts (race per-slot catch, convergence per-attempt catch + `convDeclared` last-result lift, read-only `roDeclared` terminal); packages/orchestrator/src/candidateEvidence.ts `unanimousDeclaredFailure`; packages/orchestrator/src/credential-cooldown.ts `credentialPoolExhausted` | backlog |
| engine/profiles | Profile-policy `limit_action: ask` interactive UX (2.1) | works-with-caveats | Under the unified account model an explicit PIN refuses typed on a fresh breach or observed live block for EVERY limit_action (D-U6 — pins never rotate), and unpinned runs route through the quota-aware pool; `ask` therefore only governs the LEGACY default subject (a harness with no registered rows), where the engine records the typed `route.profile.headroom_exceeded` breach and PROCEEDS — no surface actually asks the user yet (the router consumer shipped first, sol #28). `ask` is reachable only by hand-editing settings: the stored default is `auto` (kind-aware — rotate for subscription subjects, fail for metered) and the app's tri-state auto-switch control offers Off=`fail` / Auto / On=`rotate` only, deliberately preserving a hand-configured `ask` on Off | packages/orchestrator/src/account-resolution.ts resolveAccountForRun (pinned breach throws typed for every action); packages/orchestrator/src/credential-preflight.ts preflightDefaultSubject; packages/orchestrator/src/credential-profile-rotation.ts effectiveLimitAction | backlog |
| engine/profiles | Cursor accounts via the vendor file store (unified model) | works-with-caveats | Every cursor account rides the CLI's vendor-supported file credential store (`AGENT_CLI_CREDENTIAL_STORE=file`) inside a Claudexor-owned row HOME; the HOST Keychain login is retired (D-U3) — never probed, bridged, or claimed as a route — and `CURSOR_CONFIG_DIR`/`CURSOR_DATA_DIR` stay in the run's lane HOME so sessions never mix. Store isolation is live-proven WITHOUT a login (two scratch-HOME `cursor-agent status` probes land all file-store state in the scratch store and leave the real Keychain untouched — no login/OAuth write was exercised, so the evidence covers store selection, not a login write); cursor still has no PROACTIVE vendor quota source (no poller/API), so headroom-threshold preflight has nothing to read until a limit is actually observed; since the typed cursor vendor-limit classifier landed, an observed usage-limit rejection does register a reactive `cursor_rate_limit` cooldown (day-granular vendor reset honored as end-of-day UTC) that limit-triggered rotation, pool ranking, and preflight consume — so an unpinned cursor pool row rotates reactively out of the box. The app's auto-switch control follows the current server-projected pool verdict for every harness rather than a client-side harness list; the remaining caveat is that a real bootstrap-row OAuth login + run has not been executed end-to-end under the unified model yet — it rides the release VM acceptance (host login present but never appearing; isolated login; independent second row; removal not touching the host CLI) | packages/harness-cursor/src/profile.ts (file-store env + strict routing); packages/harness-cursor/src/index.ts (file-store-only native probing); packages/harness-cursor/src/retry-signals.ts (typed vendor-limit classifier); packages/cli/src/native-login.ts (row login env + overrideless refusal); packages/harness-cursor/src/auth.test.ts | unified-accounts release program (VM acceptance) |
| engine/profiles | Differential dead-credential probe + typed `credential_unusable` evidence (A7) | works-with-caveats | On a rotation-eligible failure the engine probes the CURRENT subject as a sibling of the candidate probe (poller observations + the attempt's typed non-retryable refusals + the local doctor probe — never a spawned mini-run) and distinguishes "credential dead" from "quota spent": the verdict is a typed `route.profile.credential_unusable` run event plus a bounded self-expiring observation in the daemon's in-memory `CredentialUnusableLedger`; rotation refuses a condemned candidate at the one readiness composition point, exhaustion rows carry `rejected: credential_unusable`, and the pool terminal names the dead-credential provenance. Caveats: the ledger is process-lifetime only (a daemon restart forgets observations — the poller re-derives vendor rejections within a cycle, so the cost is at most one rediscovering attempt); the one-shot `claudexor review` CLI entry constructs its Orchestrator without the ledger, so it gets the probe + typed event but no cross-run memory; clearing a MODEL-scoped observation by success requires an exactly-matching `observed_model` (vendor display labels are not slugs), so entitlement-scoped rows usually clear by their 1h TTL or a re-login; the Accounts-surface projection of observations is deliberately deferred (owner scope 4=A) | packages/orchestrator/src/credential-differential.ts (verdict + sibling prober + readiness composition); packages/daemon/src/credential-unusable-ledger.ts (bounded TTL store + clearing contract); packages/orchestrator/src/credential-profile-rotation.ts rotateSpecOnTypedLimit (probe wiring + typed event); packages/cli/src/run-orchestrator.ts (daemon wiring) | backlog (Accounts projection = separate issue) |
| engine/accounts | Unified account model — startup migration, pool routing, bootstrap rows (this sprint) | works-with-caveats | Engine-side complete and unit-proven: crash-recoverable per-harness startup migration (reserved → registry_written → continuity_migrated → completed; typed per-harness run refusal while incomplete; rollback command as the supported downgrade path), quota-aware pool routing with thread stickiness and disclosed lane switches, strict pins, the accountPools wire authority + GET /v2/account-pools feature marker, and the provable delete contract. Caveats: (a) the INV-060 capability smoke stays DEFAULT-store-scoped — profile-targeted logins (cursor bootstrap rows, named rows) verify via their doctor probe only, so the schema's null-branch receipt rule is intact but unreachable for cursor logins; (b) live end-to-end evidence (real migration of an existing login, four-way old/new wire matrix, VM cursor isolation) rides the release acceptance battery, not unit tests; (c) the macOS app's rendering of the unified rows/pool verdict is the parallel C-app stream | packages/cli/src/accounts-unified-migration.ts (+ .test.ts battery); packages/orchestrator/src/account-pool.ts + orchestrator.ts preflightProfile; packages/cli/src/control-services.ts (delete contract); packages/schema/src/accounts-migration.ts | unified-accounts release program (Ф2 synthesis + Ф3 acceptance) |
| macos+cursor | Cursor MCP manual QA phases | works-with-caveats | Historical manual-QA waivers do not certify current acceptance. Manual phases B (Cursor tool discovery after reload) and E (agent-in-the-loop through Cursor UI) remain untested. Scripted plugin installation, protocol/schema, run lifecycle and failure-mode checks are separate evidence and do not certify these manual flows. Changes affecting those paths retain their targeted acceptance and explicit untested disposition under the current release checklist. | docs/CHECKLISTS.md Release; scripts/cursor-itest.mjs | backlog |
| acp/interactions | ACP free-text mid-run questions (v3.0.0) | works-with-caveats | ACP's permission mechanism is choice-only, so an option-less (free-text) mid-run question cannot be answered inside the editor; the surface never silently skips it — it is disclosed as turn text naming the remedy and the run stays paused until answered via `claudexor follow <run>` or `POST /v2/runs/:id/interactions/:id/answer`. Choice and multi-select questions ARE answered inline (multi-select via per-option include/skip rounds) | packages/acp-server/src/index.ts requestAnswers; packages/acp-server/src/acp.test.ts (option-less turn-text + multi-select) | backlog |
| engine/effort | Per-model reasoning-effort ladders | works-with-caveats | Effort is now discovered live and resolved per (harness, model): codex `app-server` `model/list` reports `supportedReasoningEfforts` per model, so `ultra` reaches gpt-6-astra and gpt-5.6-sol while gpt-5.4 correctly stops at `xhigh`. The Claude adapter currently uses the CLI-wide ladder parsed from `claude --help`, so `model_effort_levels` stays empty and every Claude model is held to that ladder. Newer control-handshake model metadata is not yet consumed for per-model narrowing. A model whose real ceiling is lower than that ladder is therefore not narrowed, and an over-strong request clamps at the CLI ceiling instead of the model's. Closing this requires wiring and verifying that native per-model metadata. Both probes fall back to a recorded snapshot when they cannot answer (a probe failure never fails a run), and `scripts/model-hints-freshness.mjs` warns when live ladders disagree with the snapshot | packages/harness-codex/src/effort-probe.ts (`model/list` probe + snapshot); packages/harness-claude/src/effort-probe.ts (`--help` parse, no per-model source); packages/schema/src/effort.ts (`effortLevelsForModel`); packages/core/src/effort.ts (`resolveEffort`) | backlog |
| acp/auth | ACP Terminal Auth (experimental) | works-with-caveats | Only Codex on macOS/Linux is advertised, and only when the client opts into terminal auth. The method is proactive from `initialize`; Claudexor does not yet emit `auth_required` or implement legacy `authenticate`. Claude/Cursor stay absent until their full terminal lifecycle is provable | packages/acp-server/src/auth.ts; packages/cli/src/acp-auth.ts; packages/acp-server/src/acp.test.ts | backlog |
| harness/agy | Antigravity CLI (Gemini subscription) harness | works-with-caveats | Darwin profiles lazily receive a private profile-local `Library/Keychains/login.keychain-db` before agy children; when setup succeeds this suppresses the missing-keychain prompt without sharing the host Keychain, and the vendor's file fallback remains available for an operational setup miss. The earlier three-account Darwin HOME-isolation proof covers the vendor file route; the private-keychain host-preference/item-isolation A/B proof is a separate acceptance artifact. Concurrent full runs remain unverified. The effective platform policy is narrower elsewhere: Linux uses the same config-file/HOME transport but has no live multi-account proof, while Windows stores the credential at OS-user scope, permits one enabled binding, and treats HOME as vendor-state scoping rather than a separate Google identity. A legacy over-cap Windows set fails routing/setup/quota typed and is never auto-selected or probed. `/model` doctor and `/quota` share one bounded console-free runner and exact vendor auth classifier; token-file presence is diagnostic only. Other caveats remain: `agy` has no config-dir variable, so one profile HOME accumulates every thread's conversations/brain/cache; no machine-readable account identity exists; quota windows vary by plan; the closed vendor binary requires fixture re-verification when its pin moves; and a `SUCCESS` with an empty response is a typed soft-deny error. ConPTY input records wait for each write to complete before the next is issued; the consumed sidecar marks a one-shot claim, not vendor acknowledgement. The ConPTY child explicitly receives pseudoconsole standard handles instead of duplicated parent pipes; the native login fixture requires a console stdin and exact submitted input before exit 0. Deterministic Windows CI verifies that contract; a live Windows pasted-code round trip remains separate acceptance evidence | packages/harness-agy/src/profile.ts + keychain.ts + print-command.ts (route, private store and vendor proof); packages/harness-agy/fixtures/manifest.yaml; packages/cli/src/agy-quota-source.ts; packages/cli/src/harness-installer.ts | backlog |
| harness/cursor | Cursor delegation-belt hosting (mcp.json injection) | works-with-caveats | The cursor adapter now declares `capability_profile.mcp_injection` and hosts engine-owned MCP servers by reconciling `mcp.json` inside the Claudexor-owned lane `CURSOR_CONFIG_DIR` (sidecar-manifest reconcile: previously managed entries are removed on non-delegate runs — with a deterministic engine-name fallback when the manifest is unreadable, so a stale belt cannot ride orphaned; foreign entries preserved, engine-name squatting refused typed, unreadable files preserved or quarantined with typed disclosure, host `~/.cursor` never written) plus `--approve-mcps`. Caveats: (a) the owner-set live E2E on CONCEPT-CHANGE(INV-030) was executed 2026-08-29 on a real profile lane: a real headless cursor-agent run loaded the injected belt server, listed and called it over MCP, and the belt answered with its own typed governance refusal for a missing parent run id (the sanctioned probe ran outside an engine run, so no sub-run was — correctly — created; access stayed `workspace_write`, so `requires_full_access` stays false); the full parent-run delegation hop (an engine-issued parent run id producing a live sub-run) was not exercised by that probe and remains covered by unit pins plus the typed refusal; (b) unlike claude (`mcp_servers` init receipts) and codex (typed required-MCP frames), cursor's stream-json exposes no typed per-MCP startup status the parser maps, so a belt that fails to initialize INSIDE cursor-agent is not yet detected mid-run — only pre-spawn injection failures refuse typed (`required_mcp_startup_failed`); closing that needs live/upstream evidence of cursor's MCP status surface; the same E2E showed the honest failure shape a child sees (the vendor CLI reports the server unreachable and the belt namespace absent); (c) `--approve-mcps` headless behavior was verified by that live run | packages/harness-cursor/src/mcp-config.ts (reconcile + prepare); packages/harness-cursor/src/index.ts (capability profile, runCursor wiring); packages/harness-cursor/src/mcp-config.test.ts | backlog (cursor per-MCP startup status mapping; full parent-run hop evidence) |

New rows are added the moment a non-solid feature ships (see the rule above).
Deliberate design boundaries that used to live here as "caveats" are
documented in the "Design constraints" sections of `docs/ARCHITECTURE.md`
(engine) and `docs/INTEGRATIONS.md` (host/external surfaces).
