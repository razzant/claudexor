# Feature Status Ledger

This file tracks every user-facing feature that is NOT currently in a `solid`
state. It exists so an agent touching a feature inherits the known state
instead of rediscovering it. Rule (see `CONTRIBUTING.md`): when a change
alters a feature listed here, update or delete its row in the same commit. A
feature that becomes solid loses its row — this ledger shrinks toward empty;
growth is a regression signal.

Statuses: `broken` > `dead` (wired to nothing) > `half-baked` > `suspicious` >
`works-with-caveats`. `Planned` names the stabilization-program phase that
owns the fix (`backlog` = not yet scheduled). Evidence is file:line at the
time of the audit; lines drift with edits — verify before relying on them.

Rows: **114** (broken: 1, dead: 1, half-baked: 8, suspicious: 6, works-with-caveats: 98)

| Area | Feature | Status | What is wrong / caveat | Evidence | Planned |
|---|---|---|---|---|---|
| cli/daemon/api | CLI-unknown-command | broken | Falls into `default:` → prints help, **exit 0** (verified live) | packages/cli/src/cli.ts:2168-2171 | backlog |
| engine/orchestrator | F57 orchestrate max_tool_calls | dead | Executor checks it, but the only producer hardcodes `null`; unreachable. | orchestrator.ts:4831, 5788, 5819-5826 | backlog |
| cli/daemon/api | CLI-auth-login | half-baked | Prints a static hint string per harness; no action; ignores `--json` | packages/cli/src/cli.ts:1377-1395 | backlog |
| engine/orchestrator | F15 orchestrate answer_question | half-baked | (documented) | orchestrator.ts:6038-6072; schema/orchestrate.ts:28-38 | backlog |
| engine/schema | BUD-PORTFOLIO | half-baked | (planned: v0.15 P4, D7 quotas fill RouterCandidate) | packages/schema/src/budget.ts:8-19; packages/budget/src/router.ts:30-65; packages/orchestrator/src/orchestrator.ts:1190-1201 (only harnessId/family/authMode set) | v0.15 P4 |
| engine/workspace+delivery | AR-PAIRWISE | half-baked | (reasons carry no information) | `packages/arbitration/src/arbitration.ts:132-166,303-308` | backlog |
| integrations | ACP-08 consumers | half-baked | Nothing in-repo consumes it (only cli.ts wires `acp serve`); unit-tested only; no Zed/editor config examples shipped | packages/cli/src/cli.ts:1789-1800; rg: AcpServer referenced only in cli.ts | v0.15 P5 |
| integrations | PLG-12 version skew | half-baked | Only via content drift on manual `status`/`doctor`; managed comment embeds `version=X` (plugins.ts:134-138); `CLAUDEXOR_PLUGIN_VERSION`/`CLAUDEXOR_MANAGED` env vars have **no runtime consumer** (in-file markers only); no skew warning at `mcp serve` time | packages/cli/src/plugins.ts:144-150; rg: producers only | v0.15 P5 |
| macos app | rundetail.candidates | half-baked | `task.candidates` never populated for live runs (demo-only); tab always "No candidates yet" on live races | AppModel.swift:641,728 (always `[]`); TaskDetailView.swift:343-367; DemoData.swift:46-51 | v0.15 P4 |
| macos app | rundetail.plan | half-baked | For live runs only a single `final/plan.md` artifact row (plan-mode); no live step folding | AppModel.swift:1730-1736; RunComponents.swift:76-118 | v0.15 P4 |
| engine/orchestrator | F32 protected paths + human gate | suspicious | Spec + auto-protected paths → BLOCK + NEEDS_HUMAN findings with path evidence; approvals exempt only auto-protected paths; built-in human paths not bypassable by approvals; critical risk or high-risk-without-cross-family panel escalates NEEDS_HUMAN. | orchestrator.ts:3086-3220; policy/policy.ts:41-54 | v0.15 P3 |
| engine/schema | SCH-CAP-PROFILE | suspicious | Consumed: `image_input` (vision gate), `isolation.supported_containment` + `auth.credential_transports` (keychain bridge), `access_control.readonly_mechanism` (claude readonly), `auth.supported_sources/preferred_source` (CLI display). Everything else (execution_surfaces incl. supports_*/event_schema, all 9 SessionCapabilities, all 8 OutputCapabilities, probe_command, env_vars, bypass_env_vars, requires_user_session, path_redirect_sufficient, access_control readonly/workspace_write/full/mechanism/conformance_required) is produced by adapters and consumed by nothing. | packages/schema/src/harness.ts:109-235; consumers: packages/orchestrator/src/orchestrator.ts:1127, packages/core/src/capabilities.ts:3-12, packages/cli/src/cli.ts:1074-1083; absence of other consumers verified by repo-wide grep | v0.15 P3 |
| engine/workspace+delivery | DL-ADOPT | suspicious | (conflict residue mutates tree while `applyState=not_applied`) | `packages/orchestrator/src/orchestrator.ts:2858-2910`; `packages/workspace/src/git.ts:272-292` | v0.15 P3 |
| engine/workspace+delivery | DL-DELIVER | suspicious | (conflict residue, stranding, unchecked staging) | `packages/delivery/src/index.ts:56-125` | v0.15 P3 |
| engine/workspace+delivery | DL-THREADAPPLY | suspicious | (bypasses validateApplyGate entirely) | `packages/cli/src/claudexord.ts:293-356`; `packages/control-api/src/daemon-server.ts:489-506` | v0.15 P3 |
| integrations | MCP-04 `claudexor_race` | suspicious | (multi-minute call inside a sequential stdio loop; host timeouts likely) | packages/mcp-server/src/index.ts:379, 367-374, 37-51 | v0.15 P5 |
| cli/daemon/api | API-GET-/runs | works-with-caveats | Cached summaries on state+mtime fingerprint; live waitingOnUser overlay | packages/control-api/src/daemon-server.ts:331-336, 1146-1173, 1555-1595; caveat: one bad persisted state 500s the list | backlog |
| cli/daemon/api | API-GET-/runs/:id/events | works-with-caveats | Replay from events.jsonl with persisted-seq ids (legacy → line no), `Last-Event-ID` header or `?lastEventId`; bus push + 250ms poll; heartbeats; ends on terminal event or terminal daemon state | packages/control-api/src/daemon-server.ts:1175-1264, 1328-1344; caveats: 404 for queued, no backpressure | v0.15 P3 |
| cli/daemon/api | API-GET-/threads/:id | works-with-caveats | Embedded per-turn run cards (no N+1) | packages/control-api/src/daemon-server.ts:436-466; caveat: card summarize is poisoned by one malformed persisted job state (same root cause as GET /runs) | backlog |
| cli/daemon/api | API-POST-/runs | works-with-caveats | Secret scan → strict DTO → normalize (empty prompt 400 unless specPath; contradictory web 400; live-isolation agent-only; absolute existing project root); bare threadId pre-creates turn before enqueue (202-lineage fix); inline attachment bytes rejected for non-thread enqueue; 200 started / 202 queued / 500 terminal | packages/control-api/src/daemon-server.ts:265-329, 1421-1508; caveats: arbitrary `turnId` accepted, `planRunId` silently not implemented | v0.15 P3 |
| cli/daemon/api | API-control | works-with-caveats | Audit `control.requested/applied/rejected`; 409 on terminal jobs (honesty); interrupt mapped to the same daemon cancel | packages/control-api/src/daemon-server.ts:976-1004; caveat: `interrupt` ≡ `cancel` (docs describe cooperative interrupt semantics owned by process helper) | v0.15 P3 |
| cli/daemon/api | API-decision | works-with-caveats | accept_risk/override only on blocked+patch (hash-bound artifact + audit event); revert_run divergence-fenced; accept_clean_patch through gate; rerun creates decision turn before enqueue | packages/control-api/src/daemon-server.ts:755-883; caveat: rerun rebuilds params from redacted persisted params (prompt may already be redacted/truncated-at-240 in summaries only — params raw prompt kept, OK) and re-parses persisted `turnId` | v0.15 P3 |
| cli/daemon/api | API-setup-jobs | works-with-caveats | Server-owned command allowlist (fixed profiles; client can't inject commands); install requires confirm; 15-min watchdog; login via Terminal handoff script; doctor runs in-process gateway; durable registry, mid-flight jobs failed on restart | packages/cli/src/setup-jobs.ts:27-63, 219-296, 299-397; caveats: cancel rewrites terminal states, unhandled spawn error can crash daemon | v0.15 P3 |
| cli/daemon/api | API-setup-jobs-events | works-with-caveats | Poll-driven status stream, terminal `end`; per-connection seq (no Last-Event-ID resume) | packages/control-api/src/daemon-server.ts:1045-1114 | v0.15 P3 |
| cli/daemon/api | API-spec-questions/freeze | works-with-caveats | Strict scope DTO; secret scan incl. serialized body; grounding plan run + `## Open Questions` parser; freeze validates answers, folds prior tiers into decided_tradeoffs, persists SpecPack + returns specPath | packages/control-api/src/daemon-server.ts:953-974, 2322-2328; packages/cli/src/claudexord.ts:530-631; packages/cli/src/spec.ts:191-242, 386-425; caveat: inline engine runs invisible to job registry | backlog |
| cli/daemon/api | CLI-REPL | works-with-caveats | Daemon-backed thread REPL (lazy thread creation, follow pipeline, stdin handover); read-only local fallback refuses write turns | packages/cli/src/repl.ts:98-211, 260-266; caveat: `/thread` prints dead `t.state` | v0.15 P2 |
| cli/daemon/api | CLI-arg-parsing | works-with-caveats | Unknown flags fail loudly; value flags require values; `--force`/`--dry-run` command-scoped; plugin has full allowlist; boolean flags greedily consume a following positional | packages/cli/src/args.ts:23-44, 77-103; packages/cli/src/cli.ts:1510-1612 | v0.15 P5 |
| cli/daemon/api | CLI-audit/map | works-with-caveats | audit mode; prompt optional (defaults "audit this repository") | packages/cli/src/cli.ts:1757-1759, 465, 598; caveat: `audit --json <arg>` silently drops json + arg | backlog |
| cli/daemon/api | CLI-daemon-logs | works-with-caveats | Last 40 lines of claudexord.log; missing file → "not reachable" misdiagnosis; ignores `--json` (verified live) | packages/cli/src/cli.ts:1138-1141, 1144-1147 | backlog |
| cli/daemon/api | CLI-daemon-start | works-with-caveats | Spawns detached, waits ≤15s for socket+control-api; exit 1 if not ready | packages/cli/src/cli.ts:1097-1118; caveats: duplicate-start pid lie, stdio discarded | backlog |
| cli/daemon/api | CLI-daemon-status | works-with-caveats | JSON-RPC health over socket; exit 1 unreachable | packages/cli/src/cli.ts:1127-1132; caveat: no RPC timeout → can hang | backlog |
| cli/daemon/api | CLI-daemon-stop | works-with-caveats | RPC shutdown; prints text even with `--json` | packages/cli/src/cli.ts:1133-1137 | backlog |
| cli/daemon/api | CLI-exit-codes | works-with-caveats | 0 success terminals (succeeded/no_op/ungated/review_not_run), 1 failures/blocked, 2 usage | packages/cli/src/daemon-run.ts:221-224 | v0.15 P3 |
| cli/daemon/api | CLI-follow | works-with-caveats | SSE fetch with full replay; interactive TTY answers gated on live pendingInteractions re-check; exit 1 on run.failed/blocked | packages/cli/src/live.ts:154-218, 220-268; caveats: exit 0 on dropped stream, 404 for queued jobs | v0.15 P3 |
| cli/daemon/api | CLI-json-mode | works-with-caveats | Daemon run path prints exactly one JSON object incl. `{runId,runDir,status}` bench keys; in-process path mirrors `error` key | packages/cli/src/cli.ts:747-767, 643-655; caveat: usage/daemon/auth paths leak plain text | v0.15 P5 |
| cli/daemon/api | CLI-run | works-with-caveats | `--mode` validated against 5 canonical ids; `--spec` requires gated strategy (race/attempts/until-clean) except ask/audit reject; agent → daemon-tracked enqueue; read-only modes in-process | packages/cli/src/cli.ts:1703-1734, 552-585; caveat: boolean-flag swallow | v0.15 P3 |
| cli/daemon/api | CLI-spec | works-with-caveats | Two-phase: no `--answers` → plan run + questions.json draft; with `--answers` → freeze + persist spec dir; rejects secret-like prompts | packages/cli/src/cli.ts:919-1026, 925-930; caveat: silently ignores `--attach`/`--image`/`--model`/`--max-usd` | v0.15 P2 |
| cli/daemon/api | D-cancel | works-with-caveats | Abort controller; queued flips to cancelled; unknown id still returns `{cancelled:true}` | packages/daemon/src/server.ts:196-206 | v0.15 P3 |
| cli/daemon/api | D-persistence | works-with-caveats | Atomic tmp+rename, 0600, results never persisted; running→interrupted and queued re-enqueued on restart; blocked never pruned | packages/daemon/src/server.ts:226-274, 242-256; caveats: silent-corruption wipe, no shape validation | v0.15 P2 |
| cli/daemon/api | D-runner | works-with-caveats | Shared `normalizeRunStartRequest`; isolated-thread worktree resolution (base persisted only on create); native session resume map; turn binding never fails the run | packages/cli/src/claudexord.ts:72-193; caveat: bogus threadId/turnId from direct socket callers silently unbound, claudexord.ts:114-126 | backlog |
| cli/daemon/api | D-shutdown | works-with-caveats | RPC `claudexor.shutdown` aborts in-flight runs, waits ≤5s, persists; **no SIGTERM/SIGINT handler exists** | packages/daemon/src/server.ts:100-123, 207-209; packages/cli/src/claudexord.ts:635-638 | v0.15 P3 |
| cli/daemon/api | D-token | works-with-caveats | Per-user UUID at `daemon/token` 0600; constant-time compare; never rotated | packages/daemon/src/token.ts:19-46; packages/daemon/src/server.ts:392-397 | backlog |
| engine/orchestrator | F01 ask | works-with-caveats | `runAsk` → `runReadOnlyReport` intent `explain`, sequential harness chain, first success wins, web-blocked attempts open a fallback arc to the next harness; generic failure is terminal (no fallback, primary-first honesty). Answer at `final/answer.md`. | orchestrator.ts:4771-4780, 5325-5394, 5391-5393 | v0.15 P3 |
| engine/orchestrator | F06 agent --n (race) | works-with-caveats | Upfront budget leases per slot, bounded-parallel isolated envelopes, per-candidate review, synthesis option, arbitration, winner auto-adopt into in-place tree gated on clean terminal, apply failure disclosed. | orchestrator.ts:2293-2328, 2500, 2884-2909 | v0.15 P3 |
| engine/orchestrator | F07 agent --attempts | works-with-caveats | `runConvergence` with `maxAttempts`; explicit cap always wins over per-harness `max_rounds`. | orchestrator.ts:529-531, 3992-3995 | backlog |
| engine/orchestrator | F08 agent --until-clean | works-with-caveats | No fixed cap; stops on convergence, hard budget tier, all-harness quota cooldown, prompt-fingerprint loop (3rd identical repair prompt), identical-diff+failing-gate stall (`stuck_no_progress`), or signature stall after all harnesses tried. | orchestrator.ts:3616-3620, 3668-3679, 3953-3974, 3984-4009 | v0.15 P4 |
| engine/orchestrator | F14 orchestrate auto_full | works-with-caveats | Apply goes through the single `validateApplyGate` + `deliver` path with patch-hash binding and repo identity check; refusal disclosed. | orchestrator.ts:6089-6138; delivery/gate.ts:53-86 | v0.15 P3 |
| engine/orchestrator | F17 doctor runner + cache | works-with-caveats | Per-adapter try/catch → `unavailable` report; TTL cache 90 s (ok) / 15 s (non-ok); fakes never cached; invalidation hook. | core/doctor.ts:36-72, 12-24, 45 | backlog |
| engine/orchestrator | F18 deterministic gates | works-with-caveats | `runGate` via `sh -c`, outcome by exit code only, 12 kB redacted output tails persisted on non-pass, `output_truncated` flag (no silent truncation), 600 s default timeout. | review/gates.ts:21-76 | v0.15 P3 |
| engine/orchestrator | F20 explicit reviewer panel | works-with-caveats | Preserves duplicates, fails loudly on unknown/disabled/fake/degraded harness; model verified against live inventory (with one retry) or manifest hints; effort validated against declared levels. | orchestrator.ts:611-763, 765-796 | v0.15 P2 |
| engine/orchestrator | F27 findings parse/dedupe | works-with-caveats | Fenced/balanced/bare-line JSON extraction; malformed items counted, reviewer marked unhealthy; dedupe keeps most severe, never collapses NEEDS_HUMAN. | review/findings.ts:8-131, 141-186, 208-232 | backlog |
| engine/orchestrator | F37 cancellation | works-with-caveats | Signal in `spec.extra` (duck-typed), `adapter.cancel` on abort, process-group SIGINT→SIGKILL escalation, per-mode aborted checks with `cancelledResult` terminal, reviewer immediate cancel, interaction wait released. | core/proc.ts:154-173; core/runloop.ts:26-33; orchestrator.ts:1776-1782, 2524-2527, 2079-2084; reviewEngine.ts:723-743 | v0.15 P3 |
| engine/orchestrator | F54 terminal-event guarantee | works-with-caveats | `failTerminally` net around review/arbitration; routing/context/harness failures write failure.yaml + summary + run.failed; cancel → run.failed{cancelled}. Gaps exist. | orchestrator.ts:1571-1617, 2678-2685, 3944-3946 | v0.15 P3 |
| engine/schema | BUD-SCHEMA | works-with-caveats | Leases reserved/settled/cancelled with holds and rollup; DD-27 wave guard: race slots after the first reserve `budget.estimate_usd_floor` and a non-fitting estimate is a TYPED `estimate_headroom` denial (granted work continues; only `hard_cap` stops everything). Caveat: `BudgetObservation.kind` values `used_percent/cooldown/reset_hint/manual` still have no ledger producer until the quotas phase (D7), so `headroom` returns 1. | packages/budget/src/ledger.ts reserve/remainingHeadroomUsd; packages/schema/src/config.ts estimate_usd_floor; orchestrator race reserve | v0.15 P4 |
| engine/schema | CFG-HARNESS-KNOBS | works-with-caveats | `enabled:false` excludes from pools and hard-errors explicit selection; knobs applied via `routeSpecKnobs`; unsupported → `ignored_settings` on `harness.started`. `native_options` is parsed+projected but consumed by nothing (dead knob). | packages/orchestrator/src/orchestrator.ts:1002-1011,1054-1062,1624-1672,2392-2396; dead: packages/schema/src/config.ts:97 (no adapter/engine reader) | v0.15 P2 |
| engine/schema | CFG-ROUTINGPOLICY | works-with-caveats | Only `"primary"` has behavior (pins configured primary when no pool). `"portfolio"` parses, persists, displays — and behaves identically to `"auto"`. | packages/schema/src/config.ts:6,48; packages/orchestrator/src/orchestrator.ts:954-961 | backlog |
| engine/schema | DAE-PERSIST | works-with-caveats | Atomic 0600 jobs.json; `running`→`interrupted`, `queued` re-enqueued, `blocked` never pruned; params/prompts/errors redacted at persistence. Caveat: corrupt jobs.json silently loads as empty. | packages/daemon/src/server.ts:226-274,425-457 | v0.15 P3 |
| engine/schema | DOCS-GATES | works-with-caveats | docs-truth gate covers endpoints/mode-ids/CLI-flags/version; schema:gen sync gate in CI+release; staged-field gate weak but present; knip for dead exports. Env vars and config keys are NOT covered by any gate. | scripts/docs-truth-check.mjs:1-14; .github/workflows/ci.yml:50-51; scripts/staged-field-check.mjs:1-22 | v0.15 P2 |
| engine/schema | SCH-CAP-BOOLEANS | works-with-caveats | Every declared capability now has a reader: plan/implement/create_from_scratch/review/verify/synthesize/read_files/orchestrate (intent gating), web_policy, max_turns, tool_lists, browser_tool, interactive (channel gate), effort_levels, known_models(+verified_against). The 13 dead booleans were deleted in the v0.15 triage. Caveat: quota_signal/usage_signal await their D7 quota-pipeline consumer (Phase 4). | packages/schema/src/harness.ts HarnessCapabilities; packages/gateway/src/gating.ts; orchestrator RoutedAdapter | v0.15 P4 |
| engine/schema | SCH-INTENTS | works-with-caveats | 11 canonical values (compare/arbitrate retired in the v0.15 triage, CONCEPT-CHANGE INV-010); runs use implement/create_from_scratch/repair/plan/spec/explain/audit/orchestrate/review/synthesize. Caveat: `verify` has no requester until the Phase-3 FinalVerifier consumes it (kept by locked decision). | packages/schema/src/primitives.ts Intent; packages/gateway/src/gating.ts | v0.15 P3 |
| engine/schema | SCH-ORCHESTRATE | works-with-caveats | Discriminated-union plan calls, `TOOL_RISK` + `toolRisk` fail-closed (unknown=risky), executor consumes both; `OrchestratePlanProgress` produced and projected. Caveat: `OrchestrateContract.budget.max_tool_calls` has a consumer (executor cap) but its only producer hardcodes `null` — no surface can set it. | packages/schema/src/orchestrate.ts:60-177; packages/orchestrator/src/orchestrator.ts:4828-4831,5659,5788-5823 | backlog |
| engine/schema | SCH-STAGEDGATE | works-with-caveats | Gate v2 strips `//` and `/* */` comments before token search, so prose mentions no longer count as consumers; requires the field token in non-schema TS. Remaining caveat: an adapter's own capability declaration still counts (token-level check, not data-flow). | scripts/staged-field-check.mjs (comment stripping + allowlist); verified pass 437 fields | v0.15 P3 |
| engine/schema | SEC-DISABLE | works-with-caveats | Honored by codex and claude only; cursor, opencode, raw-api still call `resolveSecret` unconditionally — the kill-switch covers 2 of 5 adapters. | packages/harness-codex/src/index.ts:29; packages/harness-claude/src/index.ts:87-93; packages/harness-cursor/src/index.ts:143; packages/harness-opencode/src/index.ts:45-47; packages/harness-raw-api/src/index.ts:63-65 | backlog |
| engine/schema | SEC-LIST-DTO | works-with-caveats | Store lists names only; `SecretMetadata.harnesses/env/description` fields exist in the DTO but are never produced by anything (always absent). | packages/schema/src/control.ts:903-917; packages/cli/src/claudexord.ts:504-507; packages/secrets/src/index.ts:112-127 | v0.15 P3 |
| engine/schema | UTIL-FILEIO | works-with-caveats | `ensureDir` 0700, `writeText`/`appendLine` 0600; `readTextSafe`/`readJsonSafe` return null on ANY error (missing vs corrupt indistinguishable ); mode not re-applied to pre-existing files. | packages/util/src/index.ts:51-97 | backlog |
| engine/schema | UTIL-REDACT | works-with-caveats | 15 regex patterns (GitHub/GitLab/Anthropic/OpenRouter/OpenAI/Google/xAI/AWS/Slack/Cursor/Bearer/JWT); applied at event log, jobs.json, thread store, failure writer, artifacts. Gaps: PEM blocks, `ya29.`, `xoxe-/xoxc-`, `npm_`. | packages/util/src/index.ts:135-166; packages/event-log/src/index.ts:155-164; packages/daemon/src/server.ts:425-457; packages/daemon/src/threads.ts:243 | v0.15 P3 |
| engine/workspace+delivery | AR-RANK | works-with-caveats | (acceptance is all-or-nothing proxy; held-out/latency axes have no producer) | `packages/arbitration/src/arbitration.ts:80-106` | backlog |
| engine/workspace+delivery | AS-STORE | works-with-caveats | (`budget.jsonl` + `harnesses/` declared but never written; non-atomic writes) | `packages/artifact-store/src/index.ts:39-100` | v0.15 P3 |
| engine/workspace+delivery | BG-LEDGER | works-with-caveats | (unknown-lease settle, double-settle unguarded) | `packages/budget/src/ledger.ts:50-205` | backlog |
| engine/workspace+delivery | BG-LOOP | works-with-caveats | (only the convergence loop uses it) | `packages/budget/src/ledger.ts:40-43,196-204`; `packages/orchestrator/src/orchestrator.ts:3666-3679` | backlog |
| engine/workspace+delivery | BG-ROUTER | works-with-caveats | (weights are invented constants; latency/cost rarely supplied) | `packages/budget/src/router.ts:30-79` | backlog |
| engine/workspace+delivery | CX-ATLAS | works-with-caveats | (walk follows symlinks; bytes-vs-chars estimate drift) | `packages/context/src/atlas.ts:65-200` | backlog |
| engine/workspace+delivery | IV-DIFF | works-with-caveats | (daemon `specFreeze` never passes `previous`, so `changes` is always `[]` there) | `packages/interview/src/spec.ts:95-130`; `packages/cli/src/claudexord.ts:621` | v0.15 P3 |
| engine/workspace+delivery | IV-FREEZE | works-with-caveats | (hash never verified downstream; multi-file persist non-atomic) | `packages/cli/src/spec.ts:410-475`; `packages/util/src/index.ts:30-49` | backlog |
| engine/workspace+delivery | IV-PARSE | works-with-caveats | (documented as delimiter parsing, not governance; edge brittleness) | `packages/cli/src/spec.ts:141-305` | backlog |
| engine/workspace+delivery | WS-DIFFSTAGED | works-with-caveats | Captured-diff fidelity unverified for CRLF round-trips and binary patches (integration tests land v0.15 P3, INV-041). | `packages/workspace/src/git.ts:303-324` | v0.15 P3 |
| engine/workspace+delivery | WS-DISPOSE | works-with-caveats | (trusts `worktree_path` for `git worktree remove`; no crash GC) | `packages/workspace/src/manager.ts:315-369` | v0.15 P2 |
| engine/workspace+delivery | WS-INPLACE | works-with-caveats | Captured-diff CRLF corruption risk (INV-041, tests land v0.15 P3); `dirty_policy` is recorded but not enforced. | `packages/workspace/src/manager.ts:112-140,286-313` | v0.15 P3 |
| engine/workspace+delivery | WS-NONGIT | works-with-caveats | best-effort `cpSync` baseline, `diff -ruN` vs live tree, 200k cap with in-band truncation marker | `packages/workspace/src/manager.ts:195-215,295-310` | v0.15 P2 |
| engine/workspace+delivery | WS-PORTS | works-with-caveats | (documented TOCTOU) | `packages/workspace/src/ports.ts:3-20` | backlog |
| engine/workspace+delivery | WS-REVERT | works-with-caveats | (quoted-path removals fail silently) | `packages/workspace/src/git.ts:223-261` | v0.15 P3 |
| engine/workspace+delivery | WS-SELFIGNORE | works-with-caveats | (never repaired if content differs) | `packages/workspace/src/manager.ts:79-92`; `packages/workspace/src/thread-tree.ts:41-51` | backlog |
| engine/workspace+delivery | WS-THREADTREE | works-with-caveats | (partial-create orphan; "turns never commit" unenforced) | `packages/workspace/src/thread-tree.ts:26-54` | v0.15 P3 |
| harness adapters | F12 cursor parser | works-with-caveats | (no rate-limit/transient typing; synthetic fixtures only) | `harness-cursor/src/parse.ts:7-178`; fixtures `harness-cursor/fixtures/*.jsonl` | v0.15 P5 |
| harness adapters | F13 opencode adapter | works-with-caveats | (throw-vs-yield asymmetry; cross-provider key fallback) | `harness-opencode/src/index.ts:11-27,174-224` | backlog |
| harness adapters | F14 opencode parser | works-with-caveats | (heuristic name/kind matching; synthetic fixtures only; opencode not installed to verify) | `harness-opencode/src/parse.ts:25-118` | v0.15 P5 |
| harness adapters | F15 raw-api adapter | works-with-caveats | (no cost → ledger $0; `version: defaultModel`) | `harness-raw-api/src/index.ts:48-253` | v0.15 P2 |
| harness adapters | F24 browser full-access + off guard | works-with-caveats | (codex adapter itself has no off-guard; claude does) | `orchestrator.ts:835-853`; `harness-claude/src/index.ts:380-384` vs `harness-codex/src/index.ts:207-219` | v0.15 P5 |
| harness adapters | F25 doctor cache | works-with-caveats | (key ignores env/authPreference) | `core/src/doctor.ts:12-53` | backlog |
| harness adapters | F9 cursor doctor + keychain bridge | works-with-caveats | (bridge pierces isolation; smoke cache per instance ) | `harness-cursor/src/index.ts:283-302,327-342,344-383,664-687` | backlog |
| integrations | ACP-01 initialize | works-with-caveats | Replies `protocolVersion:1`, `agentInfo`, `agentCapabilities.promptCapabilities{embeddedContext:true}`; **omits `authMethods` and `mcpCapabilities`** required/expected by the ACP v1 InitializeResponse; ignores client's requested version (spec-legal) | packages/acp-server/src/index.ts:16, 127-133; ACP spec (agentclientprotocol.com/protocol/v1/initialization) | v0.15 P5 |
| integrations | ACP-06 interactive questions | works-with-caveats | (permission request cites a toolCallId that was never announced as a tool_call) | packages/acp-server/src/index.ts:92-94, 366-413, 387-390; acp.test.ts:389 | v0.15 P5 |
| integrations | BEN-02 swe-bench suite | works-with-caveats | (operator-run only) | benchmarks/swe-bench/scripts/run-dataset.sh:24-38 | backlog |
| integrations | BEN-03 terminal_bench suite | works-with-caveats | (Rosetta caveats documented; one stale header comment) | benchmarks/terminal_bench/claudexor_agent.py:179-268, 300-366; README.md:44-70 | v0.15 P3 |
| integrations | CI-01 ci.yml | works-with-caveats | Gates repaired in v0.15 P0 (typecheck incl. tests/scripts, staged:check, ajv, ratchet, canary, concept-gate, Swift job on PRs, scan-first ordering). Remaining: format:check ungated; real-harness battery never in CI. | .github/workflows/ci.yml | backlog |
| integrations | MCP-01 `claudexor_ask` | works-with-caveats | (blocking loop, no runId) | packages/mcp-server/src/index.ts:376, 373; cli.ts:106-168 | v0.15 P5 |
| integrations | MCP-02 `claudexor_explore` | works-with-caveats | mode=audit + swarm:true | packages/mcp-server/src/index.ts:377 | v0.15 P5 |
| integrations | MCP-03 `claudexor_run` | works-with-caveats | mode=agent; summary via `summarizeResult` | packages/mcp-server/src/index.ts:378, 280-291 | v0.15 P5 |
| integrations | MCP-05 `claudexor_plan` | works-with-caveats | mode=plan | packages/mcp-server/src/index.ts:380 | v0.15 P5 |
| integrations | MCP-06 `claudexor_create` | works-with-caveats | mode=agent + create:true | packages/mcp-server/src/index.ts:381 | v0.15 P5 |
| integrations | MCP-07 `claudexor_orchestrate` | works-with-caveats | mode=orchestrate | packages/mcp-server/src/index.ts:382 | v0.15 P5 |
| integrations | MCP-09 protocol core | works-with-caveats | initialize/ping/tools list+call; fixed protocolVersion; capabilities `{tools:{}}` (no `listChanged`); notifications ignored; malformed JSON silently skipped; **requests handled strictly sequentially** (`await this.handle(msg)` inside the read loop) | packages/mcp-server/src/index.ts:37-51, 65-112, 68-74, 44-48 | v0.15 P5 |
| integrations | MCP-11 error mapping | works-with-caveats | tool exceptions → `{content:[text], isError:true}`; unknown tool/invalid args → `-32602`; unknown method → `-32601`; no `-32700` for parse failures (silently dropped) | packages/mcp-server/src/index.ts:87-111, 44-47 | v0.15 P5 |
| integrations | PLG-05 status | works-with-caveats | Compares artifacts by exact content/state hash; drift vs unowned vs missing vs partial; **exit code always 0 for status** | packages/cli/src/plugins.ts:1331-1352, 1414-1417 | v0.15 P5 |
| integrations | PLG-08 uninstall | works-with-caveats | Removes verified-legacy shims, owned config entries (state-required for config), owned artifacts; `pruneEmptyDirs` is a deliberate NO-OP → empty dirs and `.claudexor-backups/` remain | packages/cli/src/plugins.ts:1355-1371, 1160-1190 | v0.15 P2 |
| integrations | SCR-03 staged-field | works-with-caveats | v2 (v0.15 P0): comments no longer count as consumers; runs in CI. Remaining blind spots: common-name fields unverifiable; primitives.ts declarations excluded. | scripts/staged-field-check.mjs | backlog |
| integrations | SCR-04 triad-scope-review | works-with-caveats | Exact models `openai/gpt-5.5, google/gemini-3.5-flash, anthropic/claude-opus-4.8` via OpenRouter (direct fallbacks opt-in); quorum ≥2 responded; parse_failure counts as degraded; scope reviewer must cover all 8 items or gate blocks; raw outputs never truncated; secret-like token aborts before upload; per-reviewer started/first-event/completed telemetry to `reviewer-progress.jsonl` | scripts/triad-scope-review.mjs:42-43, 654-656, 761-770, 719-721, 578-581, 587-651 | v0.15 P2 |
| integrations | SCR-05 real-harness battery | works-with-caveats | (never in CI; zero MCP/ACP/plugin coverage) | scripts/real-harness-battery.mjs:423-766, 773-822, 110-119 | v0.15 P5 |
| macos app | auth.sheet | works-with-caveats | Daemon-owned jobs polled to terminal, risk flags, timestamps, log path, confirm-install; doctor recheck after | AuthSheet.swift:28-290 | v0.15 P3 |
| macos app | composer.reviewers | works-with-caveats | Tokenized `harness[=model[:effort]]` parser; invalid tokens block Send; **server gate over-strict** | ThreadsScreen.swift:112-123, 615-628; ComposerOptionParser.swift:13-47 | v0.15 P2 |
| macos app | rundetail.artifacts | works-with-caveats | Grid of file cards, image inline/text sheet/external open; tech artifacts filtered | ArtifactGalleryView.swift:10-191 | backlog |
| macos app | rundetail.diagnostics | works-with-caveats | Failure record, engine error, web evidence, artifact texts, Copy, Open Run Folder/Daemon Log, Retry | AppModel.swift:1874-1925; TaskDetailView.swift:380-463 | v0.15 P2 |
| macos app | settings.routing | works-with-caveats | Portfolio/policy/primary/env/auth/pool + explicit Save; the global model-hint field was DELETED with `routing.default_model` (model choice is harness-scoped); primary clears via explicit JSON null. Caveat: drafts remain clobberable during concurrent saves (pre-existing). | OpsScreens.swift routing tab; Kit SettingsUpdateRequest | v0.15 P2 |
| macos app | sse.per-run | works-with-caveats | Byte-level WHATWG parser (not `.lines`), Last-Event-ID resume, seq fence, 64ms coalescing, terminal full re-snapshot | GatewayClient.swift:431-485; SSEParser.swift:24-88; AppModel.swift:1932-2020 | v0.15 P2 |

