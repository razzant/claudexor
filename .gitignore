# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.turbo/
coverage/
*.tsbuildinfo

# Claudexor repo-local runtime artifacts (canonical artifacts are committed selectively per-project)
.claudexor/*
!.claudexor/
!.claudexor/config.yaml
# Versioned per-commit review panel (D18): chooses reviewers only, reviewed like code.
!.claudexor/review-panel.yaml
.claudexor/runs/
.claudexor/workspaces/
.claudexor/cache/
.claudexor/logs/
.claudexor/local.yaml

# Adversarial review evidence packets (local by default)
.adversarial-review/

# Local operator guidance (not public repo docs)
AGENTS.md
CLAUDE.md

# Local agent/editor runtime state
.codex/
.claude/
.cursor/
.opencode/

# Local toolchain
.toolchain/

# Swift / macOS app build artifacts
.build/
.swiftpm/
DerivedData/
*.xcodeproj
*.xcworkspace

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Secrets / env
.env
.env.*
!.env.example

# Python (benchmark scripts / Harbor agent)
__pycache__/
*.pyc

# Logs / misc
*.log
*.tgz
*.db
*.sqlite
*.sqlite3
*.transcript
*.transcript.json
*.transcript.jsonl

# Local benchmark smoke outputs
benchmarks/swe-bench/logs/
benchmarks/swe-bench/gold.*.json

# macOS app distribution output
apps/macos/dist/

# Local icon-generation scratch (not product source)
.iconjobs/
