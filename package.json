{
  "name": "claudexor-monorepo",
  "version": "2.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "typecheck:tests": "tsc -p tsconfig.tests.json",
    "canary": "vitest run --config vitest.canary.config.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "schema:gen": "tsx packages/schema/scripts/gen-jsonschema.ts",
    "gen:version": "node scripts/gen-version.mjs",
    "bench:bundle": "node benchmarks/terminal_bench/scripts/bundle-cli.mjs",
    "battery:real": "node scripts/real-harness-battery.mjs",
    "docs:check": "pnpm --filter ...@claudexor/control-api build && pnpm --filter ...@claudexor/cli build && node scripts/docs-truth-check.mjs",
    "staged:check": "node scripts/staged-field-check.mjs",
    "sensitive:check": "node scripts/sensitive-resource-ownership-check.mjs",
    "knip": "knip",
    "clean": "rimraf \"packages/*/dist\" \"packages/*/*.tsbuildinfo\" .turbo \"packages/*/.turbo\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "release:workflow:check": "node scripts/release-workflow-check.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release:verify:node": "pnpm build && pnpm typecheck && pnpm typecheck:tests && pnpm test && pnpm schema:gen && git diff --exit-code packages/schema/generated && test -z \"$(git ls-files --others --exclude-standard packages/schema/generated)\" && node scripts/validate-generated-schemas.mjs && pnpm docs:check && node scripts/concept-gate.mjs --since-last-tag && pnpm staged:check && pnpm sensitive:check && pnpm knip && node scripts/complexity-ratchet.mjs && node scripts/model-hints-freshness.mjs --strict && node scripts/mcp-cli-parity-check.mjs && node scripts/fixture-freshness-check.mjs --strict && pnpm release:workflow:check && pnpm format:check && pnpm canary",
    "release:verify:swift": "(cd apps/macos/ClaudexorKit && PATH=\"$HOME/.swiftly/bin:$PATH\" swift test) && (cd apps/macos/ClaudexorApp && PATH=\"$HOME/.swiftly/bin:$PATH\" swift test && PATH=\"$HOME/.swiftly/bin:$PATH\" swift build)",
    "release:verify:app": "PATH=\"$HOME/.swiftly/bin:$HOME/.claudexor/node/bin:$PATH\" MAKE_DMG=1 apps/macos/scripts/build-app.sh",
    "release:verify": "pnpm release:verify:node && pnpm release:verify:swift && pnpm release:verify:app",
    "release": "pnpm release:verify"
  },
  "devDependencies": {
    "@claudexor/context": "workspace:*",
    "@changesets/cli": "^2.27.10",
    "@types/node": "^22.10.2",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "esbuild": "^0.28.1",
    "knip": "^6.16.1",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
