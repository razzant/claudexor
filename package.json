{
  "name": "claudex-monorepo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "schema:gen": "tsx packages/schema/scripts/gen-jsonschema.ts",
    "clean": "rimraf \"packages/*/dist\" \"packages/*/*.tsbuildinfo\" .turbo \"packages/*/.turbo\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release:verify": "pnpm typecheck && pnpm test && pnpm schema:gen && git diff --exit-code packages/schema/generated && pnpm build",
    "release": "pnpm release:verify && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "@types/node": "^22.10.2",
    "esbuild": "^0.27.0",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
