# Fixture provenance manifest. SYNTHETIC-ONLY today: no recorded
# real opencode stream exists yet (the route reports degraded — no isolated
# smoke proof); record one when a live route is available and stamp its
# version here. The freshness gate discloses this gap on every run.
#
# `expectations` (W3.8) are the STREAM SEMANTICS the conformance test asserts
# through core streamExpectationViolations. OpenCode is a MARKERLESS adapter:
# no typed final message — the engine's AnswerAssembly falls back to joined
# narration (the documented degradation).
cli: opencode
fixtures:
  basic-run.jsonl:
    source: synthetic
    expectations:
      final_messages: 0
      final_source: "none (markerless: narration-join fallback)"
      final_is_last_message: false
      thinking_events: 0
      delta_messages: 0
      typed_rate_limit: false
  session-resume.jsonl:
    source: synthetic
    expectations:
      final_messages: 0
      final_source: "none (markerless: narration-join fallback)"
      final_is_last_message: false
      thinking_events: 0
      delta_messages: 0
      typed_rate_limit: false
