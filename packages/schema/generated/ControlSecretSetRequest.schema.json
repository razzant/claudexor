{
  "$ref": "#/definitions/ControlSecretSetRequest",
  "definitions": {
    "ControlSecretSetRequest": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Managed secret reference name."
        },
        "value": {
          "type": "string",
          "minLength": 1,
          "description": "Value accepted only by the non-journaled secret route."
        }
      },
      "required": [
        "name",
        "value"
      ],
      "additionalProperties": false,
      "description": "Write one managed secret through the daemon-owned store."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
