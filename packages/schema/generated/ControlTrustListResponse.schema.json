{
  "$ref": "#/definitions/ControlTrustListResponse",
  "definitions": {
    "ControlTrustListResponse": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "repoRoot": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Repo root recorded in the trust file; null for legacy files written before provenance stamping."
              },
              "path": {
                "type": "string",
                "description": "The user-level trust file backing this state (path disclosure, no content)."
              },
              "allowFullAccess": {
                "type": "boolean",
                "description": "Whether unsandboxed full access is allowed for the repo."
              },
              "accessDefault": {
                "type": "string",
                "enum": [
                  "readonly",
                  "workspace_write",
                  "full",
                  "external_sandbox_full",
                  "inherit_native"
                ],
                "description": "Default access profile for runs in the repo."
              },
              "testCommandGrantCount": {
                "type": "integer",
                "minimum": 0,
                "default": 0,
                "description": "Number of exact external test-command grants for this repo."
              }
            },
            "required": [
              "repoRoot",
              "path",
              "allowFullAccess",
              "accessDefault"
            ],
            "additionalProperties": false,
            "description": "Trust state of one repo from its user-level trust file."
          },
          "description": "Trust states for all known repos."
        }
      },
      "required": [
        "entries"
      ],
      "additionalProperties": false,
      "description": "Response for listing per-repo trust states."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
