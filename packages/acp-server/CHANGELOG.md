# @claudexor/acp-server

## 0.14.1

- Forward advanced `session/prompt` run controls (`reviewerPanel`, reviewer
  model/effort overrides, tests, budget, access, and protected-path approvals) to
  the shared runner instead of dropping them at the ACP boundary.
- Reject malformed advanced run controls at the ACP JSON-RPC boundary before the
  shared runner starts work.

## 0.14.0

## 0.13.3

## 0.12.1
